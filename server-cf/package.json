{
  "name": "volter-tunnel-cf",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "tokens": "node scripts/brand-tokens.mjs",
    "dev": "npm run tokens && wrangler dev --local",
    "deploy": "npm run tokens && wrangler deploy",
    "typecheck": "npm run tokens && tsc --noEmit",
    "test": "npm run tokens && vitest run"
  },
  "dependencies": {
    "@volter/identity": "^0.1.0",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "tsx": "^4.22.4",
    "typescript": "^5.0.0",
    "vitest": "^4.1.9",
    "wrangler": "^4.101.0"
  }
}
