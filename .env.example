# Local secrets for operator scripts. Copy to `.env` (gitignored) and fill in.
# Used by server-cf/scripts/cf-ratelimit.sh (auto-loaded).

# Cloudflare API token with Zone → WAF: Edit + Zone → Read (on the relay zone).
CLOUDFLARE_API_TOKEN=
